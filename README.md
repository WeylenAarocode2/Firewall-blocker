Once you have unzipped the file open "config.yaml" you need to follow the instructions to replace the placeholders with the correct info. This code works by detecting if your on the specified domain and transferring over to your mobile data automatically. To start the firewall blocker launch the application called "Mihomo" it should be started automatically. If you want to bypass more then one domain just copy the line of code and change it to a different domain.

Notes:
This only works with Android, if I receive any requests for a IOS build I will consider working on it in the future.

This uses your mobile data meaning the connection may be slow and it counts toward your data limit, but only if your connected to the network specified in the code.

This was made with the assistance of AI, all code and ideas are made by me, I only had AI make the instructions because by the time I was ready to make them I was already almost three hours into the project.
